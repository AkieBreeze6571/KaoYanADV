---
description: KaoyanAdventure 核心产品愿景与工程输出规范（本地优先/不编造/最小改动）
alwaysApply: true
---

# KaoyanAdventure / 我的考研冒险：核心规则

## North Star（产品与内容）
- 用 RPG/冒险叙事包装：学习计时、学习日志、统计与成就；目标是更容易坚持。
- 所有“战报/复盘/建议”必须严格基于**本地数据**；禁止编造或推断出本地数据中不存在的事实。
- 本地优先：Room + DataStore；离线 GGUF 放私有目录；`llmedge` 仅本地推理（不联网、不上传）。

## Engineering rules（协作与改动）
- 优先最小改动；一次 PR/一次改动只解决一个问题。
- 修改前先列 Plan（最多 6 条），明确涉及文件与风险点。
- 修改后必须给出：改动文件列表 + 关键 diff 摘要 + 验证步骤（不得声称“已运行通过”）。
- UI 调整只改 presentation（Compose/UI/资源表现），不破坏数据结构与业务逻辑/状态流。

## 教练输出（Coach）硬约束
- 不引入 JSON/本地数据中不存在的**新数字/新科目/新事实**。
- 字段缺失时输出“未知/暂无”，并给出补录建议（告诉用户缺什么、去哪里补）。
